<h1> Liste d'articles </h1>

<div class="container">
  <ul class="list-group">
    <% @articles.each do |article| %>
    <li class="list-group-item"><%= link_to article.title, article_path(article)%><%= link_to '🥵', article_path(article), method: :delete, data: { confirm: 'Are you sure?' } %></li>
    <% end %>
  </ul>
  <div class="button">
    <button type="button" class="btn btn-primary"> <%= link_to "Create new Article", new_article_path%> </button>
  </div>
</div>

